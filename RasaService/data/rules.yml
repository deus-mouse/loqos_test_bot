version: "3.1"

rules:
  - rule: Greet user
    steps:
      - intent: greet
      - action: action_handle_greet

  - rule: Fallback Rule
    steps:
      - intent: nlu_fallback
      - action: action_default_fallback